"""
Django settings for app project.

Generated by 'django-admin startproject' using Django 4.2.8.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

from pathlib import Path

import django




# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent # содержит в себе путь к корневой директории


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-9uuay&lqi($o!tbv^yj)zy1wl4&4j8bjzk0jdnz7(3+jffzrwh' # секретный ключ для безопасной передачи данных по веб-серверу

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True # переменная, которая отвечает за отображение отладочной информации при ошибке на сайте
# при запуске на реальный сервер, переменную необходимо перевести в режим False, чтобы отладочная информация не отображалась

ALLOWED_HOSTS = ['*'] # допустимые хосты, на которых может работать приложение
# '*' - означет, что приложение может работать на любом хосте


# Application definition

# список установленных приложений
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django.contrib.postgres',

    'debug_toolbar',

    'main',
    'goods',
    'users',
    'carts',
    'orders',
]

# промежуточное ПО, которое отвечает за безопасность
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',

    "debug_toolbar.middleware.DebugToolbarMiddleware",
]

# указание на url адреса приложений
ROOT_URLCONF = 'app.urls'

# шаблонизатор, который отвечает за отрисовку и наполнение html страниц
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates', # движок по умолчанию (наполнение информацией html страниц и прочее)
        'DIRS': [BASE_DIR / "templates"], # доп.пути, где искать шаблоны
        'APP_DIRS': True, 
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'app.wsgi.application' # протокол работы wsgi


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'home',
        'USER': 'home',
        'PASSWORD': 'home',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

# Валидаторы паролей
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'ru-ru'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/' # префикс к url адресу для браузера(его можно менять, но есть ли в этом необходимость?)

STATICFILES_DIRS = [
    BASE_DIR / 'static' # указываю путь к общей static для всего проекта в корневой директории
    ]

MEDIA_URL = 'media/'
MEDIA_ROOT = BASE_DIR / 'media/' # константа, которая хранит изображения, дабы django их нашёл

INTERNAL_IPS = [
    "127.0.0.1",
]

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

AUTH_USER_MODEL = 'users.User' # говорю django, чтобы он определял именно мою таблицу auth_user
LOGIN_URL = '/user/login/' # переопределяю url маршрут для функции login_required в директории users, файле views.py
LOGIN_REDIRECT_URL = '/'